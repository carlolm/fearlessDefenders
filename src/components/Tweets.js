import React from 'react';

import './css/Tweets.css';

const Tweets = ({}) => (
  <div className="tweets">
    <h2>Tweets</h2>
  </div>
);

Tweets.propTypes = {
  // : React.PropTypes.
};

export default Tweets;
