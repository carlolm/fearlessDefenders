import React from 'react';

import './css/Main.css';


const Main = ({}) => (
  <div className="main">
    <h2>Main</h2>
  </div>
);

Main.propTypes = {
  // : React.PropTypes.
};

export default Main;
